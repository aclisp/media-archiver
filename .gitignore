# Weibo credentials — never commit (live session cookie)
WEIBO_COOKIE.txt

# Archive output
weibo-archive/

# Logs
*.log

# Environment files (defense-in-depth; .env not auto-loaded in v1)
.env
.env.*
!.env.example

# macOS
.DS_Store

# Node / Bun dependencies (if added later)
node_modules/
.bun/
